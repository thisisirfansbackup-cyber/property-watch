# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Runtime data (config.json/state.json/index.html are committed on purpose:
# CI needs them, state doubles as the audit trail, index.html feeds Pages)
config.local.json
sold_cache.json
epc_cache.json
web_additions.json
alerts.log
state.json.bak
.pytest_cache/
